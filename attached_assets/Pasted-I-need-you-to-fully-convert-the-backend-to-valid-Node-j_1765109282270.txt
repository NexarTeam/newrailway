I need you to fully convert the backend to valid Node.js CommonJS JavaScript so it can run on Railway without TypeScript errors.

Please do the following across the ENTIRE backend folder:

=====================================================
🔴 1. Find and REMOVE all TypeScript-only syntax:

This includes:

import type { ... } from "..."

any TS type annotations like : Request, : Response, etc.

any interfaces or types

any .ts file contents that cannot run in JS

=====================================================
🔴 2. Convert ALL ES module imports to CommonJS

Replace:

import express from "express";
import something from "./file.js";

WITH:

const express = require("express");
const something = require("./file.js");

=====================================================
🔴 3. Ensure every file uses module.exports / require()

Replace:

export default router;

WITH:

module.exports = router;

=====================================================
🔴 4. Fix the specific crash in routes.js

Open routes.js and:

delete import type { Express } from "express";

replace all imports with CommonJS require()

ensure router is defined as:

const express = require("express");
const router = express.Router();

=====================================================
🔴 5. Check ALL backend files for TS syntax

Scan these files and fix them:

index.js

routes.js

shared/*.ts (convert to .js OR rewrite contents)

utils/*

middleware/*

controllers/* (if any exist)

=====================================================
🔴 6. Convert any .ts file to .js

Rename:

shared/schema.ts → shared/schema.js

Rewrite contents to valid JavaScript.

=====================================================
🔴 7. Do NOT change logic. Only remove TS and convert imports.
=====================================================
🔴 8. When finished, tell me:

Which files were modified

Whether all TypeScript syntax has been removed

Whether backend is now fully CommonJS compatible

Do NOT modify frontend files — backend only.